const Background = ({ children }) => {
  return <div className='flex items-center justify-center w-screen h-screen bg-center bg-cover bg-auth dark:text-white'>{children}</div>;
};

export default Background;
